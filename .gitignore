# Claude Code orchestration temp files (generated during workflow)
.claude/temp/
!.claude/temp/.gitkeep

# Research files (ephemeral - distill into CLAUDE.md rules, then discard)
.claude/research/

# Local settings
.claude/settings.local.json

# Antigravity CLI local project symlinks (per-machine, points into ~/.gemini/config)
.antigravitycli/
