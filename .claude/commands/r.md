# /r — short for /research

First print exactly this help line:

`/r — short for /research: multi-agent research over codebase + optional Antigravity/Codex.`

Then invoke the `research` skill (Skill tool) with the arguments below passed through verbatim. Follow only the skill file; the help line is already printed, so skip the skill's own help-banner step.

$ARGUMENTS
