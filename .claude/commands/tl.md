# /tl — tweet-size TLDR of the last output, with action buttons

First print exactly this help line:

`/tl — short for /tldr: tweet-size summary of the last assistant output, with clickable next-action buttons.`

Then invoke the `tldr` skill (Skill tool) with the arguments below passed through verbatim. Follow only the skill file; the help line is already printed, so skip the skill's own help-banner step.

$ARGUMENTS
